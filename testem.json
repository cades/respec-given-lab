{
  "framework": "mocha",
  "launch_in_dev": ["SAFE"],
  "launch_in_ci": ["SAFE"],
  "launchers": {
    "SAFE": {
      "command": "lab -r tap",
      "protocol": "tap"
    }
  },
  "src_files": [
    "test/**/*.js",
    "test/**/*.coffee",
    "lib/**/*.js"
  ]
}
